server:
  port: ${PORT:8080}

spring:
  application:
    name: wms-lite
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

security:
  jwt:
    secret: ${zX7vB9nM2kL4pQ6rT8wY0uI2oP5aS7dF9gH1jK3mN5bV7cC9xZ1aS3dF5gH7jK9m}
    access-token-validity-seconds: ${JWT_ACCESS_TOKEN_VALIDITY_SECONDS:3600}
    refresh-token-validity-seconds: ${JWT_REFRESH_TOKEN_VALIDITY_SECONDS:1209600}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}
  seed-data:
    enabled: ${SEED_DATA_ENABLED:true}
    mode: ${SEED_DATA_MODE:auto} 

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    groups-order: DESC
    operationsSorter: method
    disable-swagger-default-url: true
  api-docs:
    path: /v3/api-docs
